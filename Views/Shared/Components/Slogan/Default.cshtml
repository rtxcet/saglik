@model List<saglik.Models.Slogan>

<div class="top-banner">
    <div class="marquee" id="bannerText">
        @for (int i = 0; i < Model.Count; i++)
        {
            var slogan = Model[i];
            <p class="@(i == 0 ? "active" : "")">@slogan.Title</p>
        }
    </div>
</div>